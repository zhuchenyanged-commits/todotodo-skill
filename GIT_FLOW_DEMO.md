# Git Flow 完整演示总结

## 🎯 演示项目：待办事项清单 Skill

**项目路径**: `/Users/eddie.zhu/skills/todo-skill/`

---

## 📊 完整的 Git Flow 流程

### ✅ 阶段 1: 项目初始化

```bash
mkdir todo-skill
cd todo-skill
git init
# 创建文件...
git add .
git commit -m "chore: 初始化项目"
```

**结果**:
- ✅ 创建了 main 分支
- ✅ 提交了初始代码

---

### ✅ 阶段 2: 建立 Git Flow 分支结构

```bash
git checkout -b develop
```

**分支结构**:
```
main     (生产环境)
develop  (开发环境)
```

---

### ✅ 阶段 3: 发布 v1.0.0

#### 3.1 创建 release 分支
```bash
git checkout develop
git checkout -b release/v1.0.0
# 添加 VERSION 文件...
git commit -m "chore: 准备发布 v1.0.0"
```

#### 3.2 合并到 main 并打标签
```bash
git checkout main
git merge release/v1.0.0
```

#### 🎯 重点：创建标签
```bash
git tag -a v1.0.0 -m "Release v1.0.0 - 首个稳定版本..."
```

**标签类型说明**:
- **轻量标签**: `git tag v1.0.0` (只是一个引用)
- **附注标签**: `git tag -a v1.0.0 -m "..."` (包含标签信息、日期、作者)

#### 3.3 合并回 develop
```bash
git checkout develop
git merge release/v1.0.0
```

**结果**:
- ✅ main 分支: v1.0.0 (稳定)
- ✅ develop 分支: 同步最新代码
- ✅ v1.0.0 标签: 标记可回溯的版本

---

### ✅ 阶段 4: 开发 v2.0 新功能

#### 4.1 创建 feature 分支
```bash
git checkout develop
git checkout -b feature/v2-priority-and-due-date
```

#### 4.2 开发新功能
```python
# 修改 todo.py
- 添加任务优先级
- 添加到期日期
- 优化显示效果
```

#### 4.3 提交并合并到 develop
```bash
git add .
git commit -m "feat: 添加任务优先级和到期日期功能 (v2.0)"
git checkout develop
git merge feature/v2-priority-and-due-date
```

---

### ✅ 阶段 5: 发布 v2.0.0

#### 5.1 创建 release 分支
```bash
git checkout develop
git checkout -b release/v2.0.0
# 更新 VERSION 文件...
git commit -m "chore: 准备发布 v2.0.0"
```

#### 5.2 合并到 main 并打标签
```bash
git checkout main
git merge release/v2.0.0
```

#### 🎯 重点：创建 v2.0.0 标签
```bash
git tag -a v2.0.0 -m "Release v2.0.0 - 重大升级..."
```

#### 5.3 合并回 develop
```bash
git checkout develop
git merge release/v2.0.0
```

**结果**:
- ✅ main 分支: v2.0.0 (最新稳定版)
- ✅ develop 分支: 同步 v2.0 代码
- ✅ v2.0.0 标签: 标记新版本

---

## 🏷️ 标签操作详解

### 1. 创建标签

#### 轻量标签（简单引用）
```bash
git tag v1.0.0
```

#### 附注标签（推荐）
```bash
git tag -a v1.0.0 -m "Release v1.0.0 - 详细说明"
```

**为什么推荐附注标签？**
- ✅ 包含标签信息
- ✅ 记录创建日期和作者
- ✅ 可以添加详细说明
- ✅ 可以被 GPG 签名

### 2. 查看标签

```bash
# 列出所有标签
git tag

# 查看标签详情
git show v1.0.0

# 列出匹配的标签
git tag -l "v1.*"
```

### 3. 对比标签

```bash
# 查看两个标签之间的差异
git diff v1.0.0 v2.0.0

# 查看差异统计
git diff v1.0.0 v2.0.0 --stat

# 查看某个文件在两个标签间的差异
git diff v1.0.0 v2.0.0 -- todo.py
```

### 4. 检出标签（查看旧版本代码）

```bash
# 查看标签指向的代码
git checkout v1.0.0

# 查看完后切回分支
git checkout main

# 基于标签创建新分支
git checkout -b hotfix-from-v1 v1.0.0
```

### 5. 删除标签

```bash
# 删除本地标签
git tag -d v1.0.0

# 删除远程标签
git push origin :refs/tags/v1.0.0
```

### 6. 推送标签到 GitHub

```bash
# 推送单个标签
git push origin v1.0.0

# 推送所有标签
git push origin --tags
```

---

## 📋 当前项目状态

### 分支结构
```
* develop                      (当前分支)
  feature/v2-priority-and-due-date
  main
  release/v1.0.0
  release/v2.0.0
```

### 标签列表
```
v1.0.0 - 首个稳定版本
v2.0.0 - 重大升级（优先级+到期日期）
```

### 提交历史
```
* 96fe502 (HEAD -> develop, tag: v2.0.0, release/v2.0.0, main)
|   chore: 准备发布 v2.0.0
|
* 9265c13
|   docs: 更新 README 文档至 v2.0
|
* 7e250c8 (feature/v2-priority-and-due-date)
|   feat: 添加任务优先级和到期日期功能 (v2.0)
|
* 8f2ce5b (tag: v1.0.0, release/v1.0.0)
|   chore: 准备发布 v1.0.0
|
* 8c857d0
    chore: 初始化待办事项清单项目
```

---

## 🚀 上传到 GitHub 的步骤

### 步骤 1: 在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 仓库名称: `todo-skill`
3. 描述: `待办事项清单 Skill - 演示 Git Flow 工作流`
4. 选择 Public 或 Private
5. ❌ 不要勾选初始化选项
6. 点击 "Create repository"

### 步骤 2: 添加远程仓库

```bash
cd /Users/eddie.zhu/skills/todo-skill
git remote add origin https://github.com/你的用户名/todo-skill.git
```

### 步骤 3: 推送 main 分支

```bash
git push -u origin main
```

### 步骤 4: 推送 develop 分支

```bash
git push -u origin develop
```

### 步骤 5: 推送所有标签

```bash
# 方法 1: 推送所有标签（推荐）
git push origin --tags

# 方法 2: 推送单个标签
git push origin v1.0.0
git push origin v2.0.0
```

### 步骤 6: 验证

访问你的 GitHub 仓库，确认：
- ✅ Code 页面显示所有文件
- ✅ 有 main 和 develop 两个分支
- ✅ Tags 页面显示 v1.0.0 和 v2.0.0

---

## 💡 标签命名规范

### 语义化版本
```
v主版本.次版本.补丁版本

示例:
v1.0.0  - 首个稳定版本
v1.0.1  - Bug 修复
v1.1.0  - 新增功能（向后兼容）
v2.0.0  - 重大变更（可能不兼容）
```

### 预发布版本
```
v2.0.0-alpha   - 内部测试版
v2.0.0-beta    - 公开测试版
v2.0.0-rc.1    - 候选版本 1
```

---

## 🎓 关键要点

### ✅ Git Flow 优势
1. **main 始终稳定** - 生产环境代码
2. **develop 集成开发** - 最新功能
3. **feature 隔离开发** - 不影响主线
4. **release 准备发布** - 最终测试
5. **hotfix 紧急修复** - 快速响应问题

### ✅ 标签的重要性
1. **版本标记** - 清晰的版本号
2. **可回溯** - 随时回到任何版本
3. **发布记录** - 发布历史一目了然
4. **GitHub Release** - 自动关联 Release

### ✅ 实践建议
1. **总是打标签** - 每次发布都打标签
2. **使用附注标签** - 包含详细信息
3. **推送标签** - 别忘了 `git push --tags`
4. **写清楚的说明** - 标签信息要详细

---

## 📚 常用命令速查

### 标签操作
```bash
git tag -a v1.0.0 -m "说明"      # 创建附注标签
git tag                          # 列出所有标签
git show v1.0.0                  # 查看标签详情
git diff v1.0.0 v2.0.0           # 对比两个标签
git checkout v1.0.0              # 检出标签
git push origin --tags           # 推送所有标签
```

### 分支操作
```bash
git branch -a                    # 查看所有分支
git checkout -b new-branch       # 创建并切换分支
git merge feature-branch         # 合并分支
```

### 查看操作
```bash
git log --graph --all --decorate # 查看分支图
git status                       # 查看状态
git diff                         # 查看差异
```

---

这个完整演示展示了从 0 到 1 的 Git Flow 工作流，特别详细地演示了标签的创建、查看和使用！
